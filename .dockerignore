node_modules
.next
.git
.gitignore
# No env file is read inside the image: NEXT_PUBLIC_* values arrive as
# --build-arg at build time. Excluding them all keeps secrets out of layers.
.env*
docs
*.md
Dockerfile
.dockerignore
npm-debug.log*
*.log
.gitattributes
coverage/
